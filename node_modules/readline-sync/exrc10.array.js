const readlineSync = require('readline-sync');

const frase = readlineSync.question("faca uma pergunta: ");

console.log(frase.toUpperCase().replaceAll("A", "I"), frase.length);
// troca por letras maiusculas
//conta quantos caracteres
//troca o a eo i
